import type { FC } from 'react';

export const EmptyLayoutMeta: FC = () => {
  return (
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0"
      key="viewport"
    />
  );
};
